pipeline {
    agent any

    environment {
        DOCKER_HUB   = "anas025"
        IMAGE_NAME   = "for-docker-app"
        IMAGE_TAG    = "${BRANCH_NAME}-${BUILD_NUMBER}"  // branch ke hisaab se tag
        FULL_IMAGE   = "${DOCKER_HUB}/${IMAGE_NAME}:${IMAGE_TAG}"
        LATEST_IMAGE = "${DOCKER_HUB}/${IMAGE_NAME}:${BRANCH_NAME}-latest"
        NAMESPACE    = "${BRANCH_NAME}"  // namespace bhi branch ke hisaab se
        SONAR_HOST   = "http://104.208.65.31:9001"
    }

    stages {

        stage('Checkout Application Repo') {
            steps {
                dir('app') {
                    git branch: "${BRANCH_NAME}",
                        url: 'https://github.com/flutterdeveloper2025-tech/my-app'
                }
            }
        }

        stage('Checkout Manifests Repo') {
            steps {
                dir('manifests') {
                    git branch: "${BRANCH_NAME}",
                        url: 'https://github.com/flutterdeveloper2025-tech/my-app-manifests'
                }
            }
        }

        // ✅ SonarQube scan only for dev branch
        stage('SonarQube Scan') {
            when { 
                expression { env.BRANCH_NAME == 'dev' }
            }
            steps {
                dir('app') {
                    withCredentials([string(credentialsId: 'sonarqube_token', variable: 'SONAR_AUTH_TOKEN')]) {
                        sh """
                        docker run --rm \
                            -v "\$PWD":/usr/src \
                            -v "\$HOME/.sonar/cache":/root/.sonar/cache \
                            sonarsource/sonar-scanner-cli \
                            -Dsonar.projectKey=my-app-${BRANCH_NAME} \
                            -Dsonar.sources=. \
                            -Dsonar.host.url=$SONAR_HOST \
                            -Dsonar.login=\$SONAR_AUTH_TOKEN
                        """
                    }
                }
            }
        }

        stage('Quality Gate') {
            when { 
                expression { env.BRANCH_NAME == 'dev' }
            }
            steps {
                timeout(time: 5, unit: 'MINUTES') {
                    waitForQualityGate abortPipeline: true
                }
            }
        }

        stage('Setup Kubeconfig') {
            steps {
                withCredentials([file(credentialsId: 'kubernetes_config', variable: 'KUBECONFIG_FILE')]) {
                    sh """
                    mkdir -p \$HOME/.kube
                    cp \$KUBECONFIG_FILE \$HOME/.kube/config
                    chmod 600 \$HOME/.kube/config
                    """
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                dir('app') {
                    sh """
                    docker build -t $FULL_IMAGE .
                    docker tag $FULL_IMAGE $LATEST_IMAGE
                    """
                }
            }
        }

        stage('Docker Login & Push') {
            steps {
                withCredentials([usernamePassword(credentialsId: 'docker_registry_dev', usernameVariable: 'USER', passwordVariable: 'PASS')]) {
                    sh """
                    echo \$PASS | docker login -u \$USER --password-stdin
                    docker push $FULL_IMAGE
                    docker push $LATEST_IMAGE
                    """
                }
            }
        }

        stage('Update Deployment YAML & Push') {
            steps {
                dir('manifests') {
                    withCredentials([string(credentialsId: 'github_pat', variable: 'GITHUB_PAT')]) {
                        sh """
                        sed -i 's|image:.*|image: $FULL_IMAGE|' ${BRANCH_NAME}/${BRANCH_NAME}-deployment.yaml

                        git config user.email "jenkins@example.com"
                        git config user.name "Jenkins CI"

                        git remote set-url origin https://\$GITHUB_PAT@github.com/flutterdeveloper2025-tech/my-app-manifests.git
                        if ! git diff --quiet ${BRANCH_NAME}/${BRANCH_NAME}-deployment.yaml; then
                            git add ${BRANCH_NAME}/${BRANCH_NAME}-deployment.yaml
                            git commit -m "[ci skip] Update ${BRANCH_NAME} image to $FULL_IMAGE"
                            git push origin ${BRANCH_NAME}
                        else
                            echo "✅ No changes in deployment YAML, skipping push"
                        fi
                        """
                    }
                }
            }
        }

        stage('Deploy to Kubernetes') {
            steps {
                dir('manifests') {
                    sh """
                    kubectl apply -f ${BRANCH_NAME}/${BRANCH_NAME}-deployment.yaml -n $NAMESPACE
                    kubectl apply -f ${BRANCH_NAME}/${BRANCH_NAME}-service.yaml -n $NAMESPACE
                    kubectl rollout restart deployment mywebsite -n $NAMESPACE
                    """
                }
            }
        }

        stage('Smoke Test') {
            steps {
                withEnv(["KUBECONFIG=$HOME/.kube/config"]) {
                    sh """
                    EXTERNAL_IP=\$(kubectl get svc mywebsite-service -n $NAMESPACE -o jsonpath='{.status.loadBalancer.ingress[0].ip}')
                    STATUS=\$(curl -o /dev/null -s -w "%{http_code}" http://\$EXTERNAL_IP)
                    if [ "\$STATUS" != "200" ]; then
                        echo "❌ ${BRANCH_NAME} App not reachable at \$EXTERNAL_IP"
                        exit 1
                    fi
                    echo "✅ ${BRANCH_NAME} App running successfully at \$EXTERNAL_IP"
                    """
                }
            }
        }
    }

    post {
        success { echo "🎉 ${BRANCH_NAME} deployment to Kubernetes successful" }
        failure { echo "❌ ${BRANCH_NAME} deployment failed" }
    }
}
